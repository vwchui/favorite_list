# AlertDialogAction

**Import:** `import { AlertDialogAction } from "./components/AlertDialog"`
**Category:** components
**Intent:** Confirmation dialog (destructive flows)

## Composition

`AlertDialogAction` is part of a compound component. Use together with: `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogCancel`.

All pieces import from the same path (`./components/AlertDialog`). See each sibling's `.md` for its API.

## Props

- `variant`: "primary" | "destructive"