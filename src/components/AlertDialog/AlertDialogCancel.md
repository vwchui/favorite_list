# AlertDialogCancel

**Import:** `import { AlertDialogCancel } from "./components/AlertDialog"`
**Category:** components
**Intent:** Confirmation dialog (destructive flows)

## Composition

`AlertDialogCancel` is part of a compound component. Use together with: `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogAction`.

All pieces import from the same path (`./components/AlertDialog`). See each sibling's `.md` for its API.

## Props

_(no public props)_