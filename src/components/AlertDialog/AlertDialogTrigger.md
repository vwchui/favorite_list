# AlertDialogTrigger

**Import:** `import { AlertDialogTrigger } from "./components/AlertDialog"`
**Category:** components
**Intent:** Confirmation dialog (destructive flows)

## Composition

`AlertDialogTrigger` is part of a compound component. Use together with: `AlertDialog`, `AlertDialogContent`, `AlertDialogAction`, `AlertDialogCancel`.

All pieces import from the same path (`./components/AlertDialog`). See each sibling's `.md` for its API.

## Props

- `asChild`: boolean