# AspectRatio

**Import:** `import { AspectRatio } from "./components/AspectRatio"`
**Category:** components
**Intent:** Layout primitive locking children to a fixed width/height ratio (e.g. ratio={16/9})

## Props

- `ratio`: number — width / height. e.g. `16 / 9` for video, `1` for square.