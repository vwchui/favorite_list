# BottomNav

**Import:** `import { BottomNav } from "./components/BottomNav"`
**Category:** components
**Intent:** Mobile-web bottom navigation bar

## Props

- `activeTab`: "shop" | "heart" | "user"
- `onTabChange`: (tab: "shop" | "heart" | "user") => void
- `contained`: boolean — Renders in-flow (not fixed) for use inside a patterns/documentation page