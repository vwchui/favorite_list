# CollapsibleContent

**Import:** `import { CollapsibleContent } from "./components/Collapsible"`
**Category:** components
**Intent:** Headless expand/collapse primitive (Collapsible + Trigger + Content) — manages open state only, no styling

## Composition

`CollapsibleContent` is part of a compound component. Use together with: `Collapsible`, `CollapsibleTrigger`.

All pieces import from the same path (`./components/Collapsible`). See each sibling's `.md` for its API.

## Props

_(no public props)_