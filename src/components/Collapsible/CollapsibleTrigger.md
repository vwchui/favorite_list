# CollapsibleTrigger

**Import:** `import { CollapsibleTrigger } from "./components/Collapsible"`
**Category:** components
**Intent:** Headless expand/collapse primitive (Collapsible + Trigger + Content) — manages open state only, no styling

## Composition

`CollapsibleTrigger` is part of a compound component. Use together with: `CollapsibleContent`, `Collapsible`.

All pieces import from the same path (`./components/Collapsible`). See each sibling's `.md` for its API.

## Props

- `asChild`: boolean — Reserved for API compatibility with the kit source. Slot/asChild