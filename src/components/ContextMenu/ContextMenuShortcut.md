# ContextMenuShortcut

**Import:** `import { ContextMenuShortcut } from "./components/ContextMenu"`
**Category:** components
**Intent:** Right-click menu — thin wrapper over SelectDropdown with the full ContextMenu* item set (trigger via right-click)

## Composition

`ContextMenuShortcut` is part of a compound component. Use together with: `ContextMenuTrigger`, `ContextMenuContent`, `ContextMenuItem`, `ContextMenuCheckboxItem`, `ContextMenuRadioItem`, `ContextMenuLabel`, `ContextMenuSub`, `ContextMenuSubContent`, `ContextMenuSubTrigger`, `ContextMenuRadioGroup`, `ContextMenu`, `ContextMenuSeparator`.

All pieces import from the same path (`./components/ContextMenu`). See each sibling's `.md` for its API.

## Props

_(no public props)_