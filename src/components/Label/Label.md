# Label

**Import:** `import { Label } from "./components/Label"`
**Category:** components
**Intent:** PX form label primitive (disabled via data-disabled). For core form labels, use FormLabel

## Props

- `disabled`: boolean — Visually mark as disabled (also forwarded as `data-disabled`).