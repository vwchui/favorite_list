# Menubar

**Import:** `import { Menubar } from "./components/Menubar"`
**Category:** components
**Intent:** Desktop menubar (File/Edit/View) with menus, submenus, checkbox/radio items and shortcuts (compound)

## Composition

`Menubar` is part of a compound component. Use together with: `MenubarGroup`, `MenubarTrigger`, `MenubarSubTrigger`, `MenubarSubContent`, `MenubarItem`, `MenubarLabel`, `MenubarSeparator`, `MenubarShortcut`, `MenubarMenu`, `MenubarPortal`, `MenubarSub`, `MenubarRadioGroup`, `MenubarContent`, `MenubarCheckboxItem`, `MenubarRadioItem`.

All pieces import from the same path (`./components/Menubar`). See each sibling's `.md` for its API.

## Props

_(no public props)_