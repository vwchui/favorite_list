# RadioGroupItem

**Import:** `import { RadioGroupItem } from "./components/Radio"`
**Category:** components
**Intent:** Single-select choice (share name; group via FormGroup)

## Composition

`RadioGroupItem` is part of a compound component. Use together with: `RadioGroup`.

All pieces import from the same path (`./components/Radio`). See each sibling's `.md` for its API.

## Props

- `value`: string (required) — The value this item represents.