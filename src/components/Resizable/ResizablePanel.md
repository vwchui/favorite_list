# ResizablePanel

**Import:** `import { ResizablePanel } from "./components/Resizable"`
**Category:** components

## Composition

`ResizablePanel` is part of a compound component. Use together with: `ResizablePanelGroup`, `ResizableHandle`.

All pieces import from the same path (`./components/Resizable`). See each sibling's `.md` for its API.

## Props

_(no public props)_