# ResizablePanelGroup

**Import:** `import { ResizablePanelGroup } from "./components/Resizable"`
**Category:** components

## Composition

`ResizablePanelGroup` is part of a compound component. Use together with: `ResizablePanel`, `ResizableHandle`.

All pieces import from the same path (`./components/Resizable`). See each sibling's `.md` for its API.

## Props

- `direction`: "horizontal" | "vertical" — AX/PX legacy alias for 's `orientation` prop. v4 of
- `orientation`: "horizontal" | "vertical"