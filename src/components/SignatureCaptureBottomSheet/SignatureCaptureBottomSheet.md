# SignatureCaptureBottomSheet

**Import:** `import { SignatureCaptureBottomSheet } from "./components/SignatureCaptureBottomSheet"`
**Category:** components
**Intent:** SignatureCapture hosted in a BottomSheet with an Agree & sign action. For a side panel, use SignatureCapturePanel

## Props

- `isOpen`: boolean (required)
- `onClose`: () => void (required)
- `title`: string
- `onSubmit`: () => void
- `submitLabel`: string
- `submitDisabled`: boolean