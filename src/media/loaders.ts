/* Auto-generated from Airtable "All Media" view — do not edit by hand */

// Convenience barrel over the two loader modules, for callers that want both.
//
// A SHIPPED COMPONENT MUST NOT IMPORT THIS. Importing it reaches both loader
// maps, and MEDIA_IMAGE_LOADERS' dynamic imports put the PNG-bearing tenant
// modules in the bundler graph — which emits their assets even when the chunk
// is tree-shaken away. Import `./svg-loaders` or `./image-loaders` directly.

export { MEDIA_SVG_LOADERS } from './svg-loaders';
export { MEDIA_IMAGE_LOADERS } from './image-loaders';
