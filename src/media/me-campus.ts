/* Auto-generated from Airtable "All Media" view — do not edit by hand */

// Re-exported, not redefined — the SVG map and its `?raw` imports live in
// ./me-campus.svgs so the SVG loader path never reaches the PNG imports above.
export { AGENT_ME_CAMPUS_MEDIA_SVGS } from './me-campus.svgs';

/** PNG-backed assets keyed by name, imported as bundler-resolved asset file URLs.
 *  Use directly as an `<img src>` or inside `background-image: url()`. */
export const AGENT_ME_CAMPUS_MEDIA_IMAGES: Record<string, string> = {

};

/** Airtable token name (e.g. "wcp-primitive-media-logo") for each asset entry. */
export const AGENT_ME_CAMPUS_MEDIA_TOKENS: Record<string, string> = {
  Agent: "ld-primitive-icon-agent",
  AgentAvatar: "ld-primitive-media-agentAvatar",
};
