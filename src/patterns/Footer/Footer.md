# Footer

**Import:** `import { Footer } from "./patterns/Footer"`
**Category:** patterns
**Intent:** Drop-in site footer that auto-picks desktop vs mobile-web layout by viewport (override with variant)

## Props

- `variant`: "auto" | "desktop" | "mweb" — Force a specific variant. By default the wrapper picks based on viewport