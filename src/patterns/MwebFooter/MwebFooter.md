# MwebFooter

**Import:** `import { MwebFooter } from "./patterns/MwebFooter"`
**Category:** patterns
**Intent:** Mobile-web footer

## Props

- `contained`: boolean — When true, renders in-flow for use inside patterns/docs pages
- `navigationLabel`: string — Accessible name for the footer nav landmark. Override when multiple footers appear on one page.