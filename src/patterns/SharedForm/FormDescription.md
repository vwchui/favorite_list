# FormDescription

**Import:** `import { FormDescription } from "./patterns/SharedForm"`
**Category:** patterns

## Composition

`FormDescription` is part of a compound component. Use together with: `Form`, `FormField`, `FormItem`, `SharedFormLabel`, `FormControl`, `FormMessage`.

All pieces import from the same path (`./patterns/SharedForm`). See each sibling's `.md` for its API.

## Props

- `children`: ReactNode (required)