# FormItem

**Import:** `import { FormItem } from "./patterns/SharedForm"`
**Category:** patterns

## Composition

`FormItem` is part of a compound component. Use together with: `Form`, `FormField`, `SharedFormLabel`, `FormControl`, `FormDescription`, `FormMessage`.

All pieces import from the same path (`./patterns/SharedForm`). See each sibling's `.md` for its API.

## Props

- `children`: ReactNode (required)