# FormMessage

**Import:** `import { FormMessage } from "./patterns/SharedForm"`
**Category:** patterns

## Composition

`FormMessage` is part of a compound component. Use together with: `Form`, `FormField`, `FormItem`, `SharedFormLabel`, `FormControl`, `FormDescription`.

All pieces import from the same path (`./patterns/SharedForm`). See each sibling's `.md` for its API.

## Props

- `children`: ReactNode