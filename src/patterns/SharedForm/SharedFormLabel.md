# SharedFormLabel

**Import:** `import { SharedFormLabel } from "./patterns/SharedForm"`
**Category:** patterns

## Composition

`SharedFormLabel` is part of a compound component. Use together with: `Form`, `FormField`, `FormItem`, `FormControl`, `FormDescription`, `FormMessage`.

All pieces import from the same path (`./patterns/SharedForm`). See each sibling's `.md` for its API.

## Props

- `children`: ReactNode (required)
- `disabled`: boolean