# SidebarGroupContent

**Import:** `import { SidebarGroupContent } from "./patterns/Sidebar"`
**Category:** patterns
**Intent:** App navigation sidebar — composable kit (SidebarProvider + parts) or data-driven SidebarShell with hover-expand, lock, resize

## Composition

`SidebarGroupContent` is part of a compound component. Use together with: `SidebarProvider`, `SidebarTrigger`, `SidebarRail`, `SidebarHeader`, `SidebarFooter`, `SidebarSeparator`, `SidebarContent`, `SidebarGroup`, `SidebarGroupLabel`, `SidebarMenu`, `SidebarMenuItem`, `SidebarMenuButton`, `SidebarMenuSub`, `SidebarMenuSubItem`, `SidebarMenuSubButton`, `Sidebar`, `SidebarInset`, `SidebarShell`.

All pieces import from the same path (`./patterns/Sidebar`). See each sibling's `.md` for its API.

## Props

_(no public props)_